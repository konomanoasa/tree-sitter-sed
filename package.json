{
  "name": "tree-sitter-posix-sed",
  "version": "0.1.0",
  "private": true,
  "description": "Tree-sitter BRE and ERE grammars for POSIX.1-2024 sed scripts.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/konomanoasa/tree-sitter-posix-sed.git"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=22.0.0",
      "onFail": "error"
    },
    "packageManager": {
      "name": "npm",
      "version": ">=10.9.0",
      "onFail": "error"
    }
  },
  "scripts": {
    "cli": "node scripts/run-tree-sitter.js",
    "generate": "node scripts/generate-parsers.js",
    "format": "biome format --write .",
    "check:biome": "biome check .",
    "check:generated": "node scripts/check-generated.js",
    "test": "npm run test:corpus",
    "test:corpus": "node scripts/run-tree-sitter.js test",
    "test:debug": "node scripts/run-tree-sitter.js test --debug-build --rebuild",
    "test:fuzz": "node scripts/run-fuzz-tests.js --iterations 100 --edits 5",
    "check": "npm run check:biome && npm run check:generated && npm test"
  },
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "tree-sitter-cli": "0.26.11"
  },
  "allowScripts": {
    "tree-sitter-cli": true
  }
}
